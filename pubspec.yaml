name: markdowner
description: "A high-performance Flutter markdown WYSIWYG widget with Typora-style reveal/hide editing."
version: 0.1.0
homepage:

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  html: ^0.15.5
  petitparser: ^7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/markdowner.png
